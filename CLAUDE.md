# ALTERNATIVE

聖地巡礼の「計画 → 現地 → 記録」をひとつで回す個人用Webアプリ。
名前はマブラヴ オルタネイティヴ由来（積み重ねて、より良い自分へ）。

**最大の目標：2026/12/25〜27の佐渡島巡礼で、このアプリを実際に使うこと。**

---

## このリポジトリで作業するときの前提

### 開発方針

- **コードは本人（オーナー）が書く。** Claudeは設計相談・エラー調査・レビュー・リファクタ提案を担当する。
  機能実装をまるごと代筆しない。「なぜそう作ったか」を本人が説明できる状態を保つため。
- 詰まったときは遠慮なく相談する。それは理解の一部。

### スコープ規律（最重要）

このプロジェクトの最大のリスクは **機能を増やしすぎて完成しないこと**。

- **機能を思いついたら、実装せずREADMEの「今後やりたいこと」に書く。**
- 機能を足すなら、必ず何かを削る。総量を増やさない。
- 「完璧」より「動いて公開されている」を優先する。

---

## 技術スタック

| 層 | 使うもの | メモ |
|---|---|---|
| 本体 | **Flask** | 学習の継続性を優先。Djangoには乗り換えない |
| テンプレート | Jinja2 | `templates/` |
| DB | **Flask-SQLAlchemy** + SQLite（開発） | 本番はPostgreSQL |
| マイグレーション | **Flask-Migrate** | |
| 認証 | **Flask-Login** | |
| フォーム | **Flask-WTF** | CSRF対策込み |
| 画像 | **Cloudinary** | PaaSはディスクが揮発するのでローカル保存は不可 |
| 地図 | **Leaflet + OpenStreetMap** | 無料・APIキー不要 |
| デプロイ | Render など | |

---

## 要件定義（MVP）

### 機能

| # | 機能 | 状態 |
|---|---|---|
| F1 | ユーザー登録・ログイン | 未 |
| F2 | 聖地スポットのCRUD（名前・作品・住所・座標・シーンメモ・優先度） | 未 |
| F3 | 訪問記録の投稿（日付・写真1枚・メモ） | 未 |
| F4 | 一覧と絞り込み（未訪問／訪問済み） | 未 |
| F5 | 地図表示（Leaflet） | 未 |
| F6 | スマホ対応（375px〜） | 未 |

### データモデル（2テーブル + User）

```
User   … id, username, password_hash

Spot   … id, user_id, name, work(文字列), address,
          lat, lng, scene_note, priority, is_visited, created_at

Visit  … id, spot_id, visited_on, photo_url, memo, created_at
```

### 画面（5枚）

1. ログイン／新規登録
2. スポット一覧（絞り込み・地図）
3. スポット詳細（訪問記録が並ぶ）
4. スポット登録・編集
5. 訪問記録の投稿

### スコープ外（実装しない。READMEの「今後」に書くだけ）

- ジオコーディング（住所→座標の自動変換）。座標はGoogleマップから手でコピペする
- オフライン対応（PWA）
- SNS・フォロー・公開機能
- 写真の複数枚投稿
- 経路探索・ルート最適化

---

## マイルストーン

基本情報技術者試験は **2026/12/06（日）受験予定**。これが12月を圧迫するので、
**開発の山場は11月に前倒しする。**

| 時期 | ゴール |
|---|---|
| 9/21〜10/4 | 免許合宿。**開発は停止** |
| 10/5〜10/25 | 環境構築・SQLAlchemy入門・F1（認証）まで |
| 10/26〜11/22 | **MVP実装の本番。F2〜F5をここで終わらせる** |
| 11/23〜12/6 | **基本情報に全振り。開発は完全に停止** |
| 12/7〜12/24 | 仕上げ・デプロイ・佐渡で使える状態にする |
| **12/25〜27** | **佐渡島で実際に使う** |
| 2027年1月 | README・テスト・応募開始 |

**12月は過密（12/6 試験 → 12/13 ライブ → 12/25 佐渡）。12月に新機能を作る前提で計画を組まないこと。**

### 完成の定義（DoD）

- 本番URLがあり、スマホから一連の操作ができる
- READMEに「なぜ作ったか」が書いてある
- 自分が実際に使った記録が入っている

---

## 既知の地雷

- **画像アップロードは本番で必ず詰まる。** Render等はデプロイのたびにディスクが消えるので、
  ローカル保存ではなくCloudinaryを使う。11月下旬にぶつかる想定。
- **佐渡は圏外の場所がある。** 現地では写真を撮るだけにして、電波のある場所で登録する運用にする。
  （オフライン対応はスコープ外。使ってみて気づいた課題としてREADMEに書く）

---

## 履歴メモ

- 2026年8月：Flask学習として開始。最小サーバー → テンプレート → フォーム → 画像表示まで到達。
- 2026年9月：聖地巡礼アプリに一本化。学習用ルート（/time /calc /greet /about）と
  旧・進捗トラッカー（/alternative /study）を削除。git履歴には残っている。
